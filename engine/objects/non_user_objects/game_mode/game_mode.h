#pragma once

namespace ge {
    enum class GameMode {
        MainMenu,
        Settings,
        AboutAuthors,
        InGame,
        RecentScript,
        Info,
        IngameMenu,
        LoadingSave
    };
}